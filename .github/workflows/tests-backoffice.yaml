name: tests-backoffice

on:
  pull_request:
    paths:
      - "backoffice/**"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: cd backoffice && npm install && npm run test:ci
