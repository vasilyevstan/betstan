name: tests-bet

on:
  pull_request:
    paths:
      - "bet/**"

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: cd bet && npm install && npm run test:ci
