name: tests-bet

on:
  pull_request:
    paths:
      - "bet/**"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: cd bet && npm install && npm run test:ci
