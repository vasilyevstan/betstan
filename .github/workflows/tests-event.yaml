name: tests-event

on:
  pull_request:
    paths:
      - "event/**"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: cd event && npm install && npm run test:ci
