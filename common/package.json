{
  "name": "@betstan/common",
  "version": "1.1.0-rc.1",
  "description": "",
  "main": "./build/index.js",
  "types": "./build/index.d.ts",
  "files": [
    "build/**/*"
  ],
  "scripts": {
    "clean": "del ./build/*",
    "build": "npm run clean && tsc -p tsconfig.json",
    "test": "npm run build && tsc --noEmit -p tsconfig.test.json && npm run test:legacy-amqp && node --test tests/*.test.js",
    "test:legacy-amqp": "tsc --noEmit -p tsconfig.legacy-amqp.json",
    "test:ci": "npm run test",
    "prepack": "npm run build",
    "prepublishOnly": "npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/amqplib": "^0.10.4",
    "@types/cookie-session": "^2.0.48",
    "@types/express": "^4.17.21",
    "@types/jsonwebtoken": "^9.0.5",
    "amqplib": "^0.10.3",
    "cookie-session": "^2.0.0",
    "express": "^4.18.2",
    "express-validator": "^7.0.1",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "del-cli": "^5.1.0",
    "legacy-amqplib-types": "npm:@types/amqplib@0.10.4",
    "legacy-common": "npm:@betstan/common@1.0.54",
    "typescript": "^5.3.3"
  }
}
