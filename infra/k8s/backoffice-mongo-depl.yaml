apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: gaming-backoffice-mongo-depl
spec:
  serviceName: gaming-backoffice-mongo-srv
  replicas: 1
  selector:
    matchLabels:
      app: gaming-backoffice-mongo
  template:
    metadata:
      labels:
        app: gaming-backoffice-mongo
    spec:
      containers:
        - name: gaming-backoffice-mongo
          image: mongo
          volumeMounts:
            - name: gaming-backoffice-mongo-data
              mountPath: /data/db
  volumeClaimTemplates:
    - metadata:
        name: gaming-backoffice-mongo-data
      spec:
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: gaming-backoffice-mongo-srv
spec:
  type: ClusterIP
  selector:
    app: gaming-backoffice-mongo
  ports:
    - name: gaming-backoffice-mongo-port
      protocol: TCP
      port: 27017
      targetPort: 27017
