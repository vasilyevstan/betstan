apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: gaming-event-mongo-depl
spec:
  serviceName: gaming-event-mongo-srv
  replicas: 1
  selector:
    matchLabels:
      app: gaming-event-mongo
  template:
    metadata:
      labels:
        app: gaming-event-mongo
    spec:
      containers:
        - name: gaming-event-mongo
          image: mongo
          volumeMounts:
            - name: gaming-event-mongo-data
              mountPath: /data/db
  volumeClaimTemplates:
    - metadata:
        name: gaming-event-mongo-data
      spec:
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: gaming-event-mongo-srv
spec:
  type: ClusterIP
  selector:
    app: gaming-event-mongo
  ports:
    - name: gaming-event-mongo-port
      protocol: TCP
      port: 27017
      targetPort: 27017
