apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: gaming-gamemaster-mongo-depl
spec:
  serviceName: gaming-gamemaster-mongo-srv
  replicas: 1
  selector:
    matchLabels:
      app: gaming-gamemaster-mongo
  template:
    metadata:
      labels:
        app: gaming-gamemaster-mongo
    spec:
      containers:
        - name: gaming-gamemaster-mongo
          image: mongo
          volumeMounts:
            - name: gaming-gamemaster-mongo-data
              mountPath: /data/db
  volumeClaimTemplates:
    - metadata:
        name: gaming-gamemaster-mongo-data
      spec:
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: gaming-gamemaster-mongo-srv
spec:
  type: ClusterIP
  selector:
    app: gaming-gamemaster-mongo
  ports:
    - name: gaming-gamemaster-mongo-port
      protocol: TCP
      port: 27017
      targetPort: 27017
