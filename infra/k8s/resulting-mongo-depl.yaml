apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: gaming-resulting-mongo-depl
spec:
  serviceName: gaming-resulting-mongo-srv
  replicas: 1
  selector:
    matchLabels:
      app: gaming-resulting-mongo
  template:
    metadata:
      labels:
        app: gaming-resulting-mongo
    spec:
      containers:
        - name: gaming-resulting-mongo
          image: mongo
          volumeMounts:
            - name: gaming-resulting-mongo-data
              mountPath: /data/db
  volumeClaimTemplates:
    - metadata:
        name: gaming-resulting-mongo-data
      spec:
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: gaming-resulting-mongo-srv
spec:
  type: ClusterIP
  selector:
    app: gaming-resulting-mongo
  ports:
    - name: gaming-resulting-mongo-port
      protocol: TCP
      port: 27017
      targetPort: 27017
