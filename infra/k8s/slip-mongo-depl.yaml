apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: gaming-slip-mongo-depl
spec:
  serviceName: gaming-slip-mongo-srv
  replicas: 1
  selector:
    matchLabels:
      app: gaming-slip-mongo
  template:
    metadata:
      labels:
        app: gaming-slip-mongo
    spec:
      containers:
        - name: gaming-slip-mongo
          image: mongo
          volumeMounts:
            - name: gaming-slip-mongo-data
              mountPath: /data/db
  volumeClaimTemplates:
    - metadata:
        name: gaming-slip-mongo-data
      spec:
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: gaming-slip-mongo-srv
spec:
  type: ClusterIP
  selector:
    app: gaming-slip-mongo
  ports:
    - name: gaming-slip-mongo-port
      protocol: TCP
      port: 27017
      targetPort: 27017
